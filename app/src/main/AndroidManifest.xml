<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jae.cst2335_final_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LivingRoomRemoteActivity" />
        <activity android:name=".AutomobileRemoteActivity" />
        <activity android:name=".HouseSettingsRemote" />

        <!-- Jacob's Classes -->
        <activity android:name=".KitchenLight" />
        <activity android:name=".Fridge" />
        <activity android:name=".Microwave" />
        <activity
            android:name=".KitchenRemote"
            android:label="@string/title_activity_kitchen_remote"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- -->

    </application>

</manifest>